#![no_std]

/// Initializes peripherals and runtime. The implementation will be filled once the
/// hardware abstraction layers are configured.
pub fn init() {
    // Placeholder for future firmware initialization steps.
}
