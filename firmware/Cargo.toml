[package]
name = "firmware"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
embassy-executor = { version = "0.2", default-features = false }
esp-hal = { version = "1.0.0-rc.1", default-features = false, features = ["esp32s3"] }
esp-hal-embassy = { version = "0.9", default-features = false, features = ["esp32s3"] }
